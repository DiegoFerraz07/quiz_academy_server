const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'), // Caminho para o arquivo de configuração do banco de dados
  'models-path': resolve(__dirname, 'src', 'models'), // Caminho onde os models (definições das tabelas) estão localizados
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), // Caminho onde ficarão as migrations (arquivos de versionamento de banco de dados)
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'), // Caminho onde ficarão os seeders (scripts para popular o banco com dados iniciais ou de teste)
};
